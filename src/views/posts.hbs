<ul class="member-list container">
  {{#each posts}}
    <li>
      <article>
        <h2>{{this.username}}</h2>
        <h4>{{prettifyDate this.date}}</h4>
        <p>{{this.post}}</p>
      </article>
    </li>
  {{/each}}
</ul>

{{> addpost}}
